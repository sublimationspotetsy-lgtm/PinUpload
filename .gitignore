# This repo is an IMAGE HOST only (raw.githubusercontent.com serves images/).
# No app source is published. Everything not explicitly allowed below is ignored.

/*
!/.gitignore
!/README.md
!/LICENSE
!/images

# Inside images/: keep the flat pin PNGs + the _archive/ subtree, ignore
# per-slug artifacts
/images/*/
!/images/_archive
.DS_Store
